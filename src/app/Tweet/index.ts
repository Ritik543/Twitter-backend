import { types } from "./types";
import { muatations } from "./mutations";
import { queries } from "./querry";
import { resolvers } from "./Resolver";

export const Tweet = { types, resolvers, muatations ,queries};